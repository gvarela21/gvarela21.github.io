// Randomize of the file name of
// the balloon to be selected. You will need to (this is pseudocode)
// a) create a random number
// b) convert that random number into a string (use the nf
// function)
// c) put that random figure into the 1st position in an array
// d) put “.png” into the second position in the array
// e) create separator and message variables and join the array f) use the new message to load image
